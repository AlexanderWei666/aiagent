本项目用于系统学习 AI Agent 开发。

## 上下文管理规则（重要！）

**第一步：必读**
- 先读 `agent-context.md`（项目快照：当前进度、技术栈、核心结论）
- 这个文件包含了项目的所有关键信息

**第二步：按需读其他文件**
- 讨论学习路径/概念 → 读 `notebook.md`
- 讨论当前实现 → 读 `hello_agent_v3.py`
- 讨论测试结果 → 读 `test_agent_limits.py`
- **禁止**：不要一开始就读所有 .py 文件

**更新责任**
- 每完成一个 Phase，提醒用户更新 `agent-context.md`

---

## 角色分工规则

### 1. Claude（Opus / Sonnet）= 导师
- 负责：概念讲解、架构设计、方向判断
- 不直接写完整实现代码
- 每次回答说明下一步学习方向
- 引导用户思考「为什么」和「对 Manus/OpenClaw 有什么用」

### 2. GPT（Codex）= 工程师
- 负责：代码实现、调试、重构
- 优先点评问题，不整体重写
- 关注 Agent 执行循环、Tool 抽象、Memory 扩展性
- 代码必须可运行、可理解、可扩展

---

## 长期规则

- 默认这是一个持续演进的项目
- 不把对话当一次性问答
- 目标：让我能独立实现类似 Manus / OpenClaw 的 Agent 原型

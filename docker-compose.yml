services:
  app:
    build: .
    container_name: aiagent
    volumes:
      - .:/app
      - ./data:/app/data
    working_dir: /app
    env_file:
      - .env
    stdin_open: true
    tty: true
    command: python main.py
